{% comment %}
  Installed immediately before </body> in the active Shopify theme's
  layout/theme.liquid file. This corrects stale cached page copies without
  affecting any other page or link.
{% endcomment %}
{% if request.path == '/pages/free-ai-writing-tool' %}
  <script>
    (function ensureStarWriterDownloaderLink() {
      const downloadUrl = 'https://github.com/billystarbuck/Star-Writer/releases/download/v0.76-beta/Star.Writer.Beta.Downloader.exe';
      const liteDownloadUrl = 'https://github.com/billystarbuck/Star-Writer/releases/download/v0.76-lite-beta/Star.Writer.Lite.Beta.Downloader.exe';
      const updateDownloadLink = () => {
        document.querySelectorAll('a').forEach((link) => {
          if (link.textContent.trim() === 'Download Star Writer 0.76 Beta') {
            link.href = downloadUrl;
          }
          if (link.textContent.trim() === 'Download Star Writer Lite 0.76 Beta') {
            link.href = liteDownloadUrl;
          }
        });
      };

      document.addEventListener('DOMContentLoaded', updateDownloadLink);
      document.addEventListener('shopify:section:load', updateDownloadLink);
      updateDownloadLink();
    })();
  </script>
{% endif %}
