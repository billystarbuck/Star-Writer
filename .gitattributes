# Auto detect text files and perform LF normalization
* text=auto

# The bundled AI payload is split below GitHub's per-file LFS limit.
"Star Writer 0.74/*.payload.zip.part*" filter=lfs diff=lfs merge=lfs -text
